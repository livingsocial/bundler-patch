require 'spec_helper'

describe Bundler::Patch do
  it 'has a version number' do
    expect(Bundler::Patch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
